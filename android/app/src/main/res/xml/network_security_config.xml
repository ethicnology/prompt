<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dart rejects HTTP unless its address is a private WireGuard address. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
